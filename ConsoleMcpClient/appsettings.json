{
  "UseLocalModel": true,
  // ApiKey is used for Azure OpenAI Service, and it should be set to the value of the API key for your Azure OpenAI resource.
  "ApiKey": "x",
  // LlmEndpoint is used for local models hosted on Ollama and Azure OpenAI Service.
  // For Ollama, it should be in a format like "http://localhost:{port}/v1".
  // For Azure OpenAI Service, it should be in a format like "https://{your-resource-name}.openai.azure.com/".
  "LlmEndpoint": "http://localhost:11434/v1",
  "McpEndpoint": "https://localhost:5106",
  // ModelId is used when UseLocalModel is true, and it should match the model ID in the local model registry.
  "ModelId": "llama3.2:1b",
  // Deployment name is used when UseLocalModel is false, It should match the deployment name in Azure OpenAI Service.
  "DeploymentName": "gpt-5-nano"
}
